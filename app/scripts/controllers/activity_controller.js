/*global define*/

define([
	'jquery',
	'underscore',
	'backbone.marionette',
	'utils/dispatcher'
], function ($, _, Marionette, Dispatcher) {
	'use strict';

	var Controller = Marionette.Controller.extend({
		
	});

	return Controller;
});
